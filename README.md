# audio-codec-vhd-communication
A VHDL-based implementation of a Parallel-to-Serial converter enabling synchronous serial communication between an FPGA and an audio codec, verified through ModelSim simulation and hardware testing
